{
  "name": "k-terminal-finance",
  "version": "1.0.0",
  "private": true,
  "description": "Korean high-density crypto trading terminal — live crypto markets, news, portfolio, AI, and an auto-dom execution cockpit.",
  "type": "module",
  "scripts": {
    "start": "node src/server.js",
    "dev": "NODE_ENV=development node src/server.js",
    "test": "node --test test/*.mjs",
    "check": "node --check src/server.js && node --check public/app.js && node --check src/stream.js && node --check src/autodom.js && node --check src/notifications.js && node --check src/cryptoMarket.js",
    "smoke": "node scripts/smoke-test.mjs",
    "start:all": "bash scripts/run-all.sh"
  },
  "engines": {
    "node": ">=20.10"
  }
}
